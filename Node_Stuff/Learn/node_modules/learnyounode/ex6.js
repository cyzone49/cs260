let mymodule = require("./ex6_module.js");

function callback( err, content ) {
    if (err) return console.error(err);
    
    for( let i = 0; i < content.length; ++i) {
        console.log( content[i]);
    }
    
}

mymodule( process.argv[2], process.argv[3], callback );